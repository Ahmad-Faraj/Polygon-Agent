#include <bits/stdc++.h>
using namespace std;

// <<<PF_PLACEHOLDER>>>
// Brute-force: correct but intentionally slow.
// Rules:
//   MUST produce IDENTICAL output to acc.cpp on all valid inputs.
//   Tagged TL (Time Limit) on Polygon -- slow is expected.
//   Used for stress testing: diff <(./brute < test) <(./acc < test)
//   Clarity > speed. Make it obviously correct.
//   Typical approaches: enumerate subsets (1<<n), nested loops, O(n^2)/O(n^3).

void Solve() {
    // <<<PF_PLACEHOLDER>>>
    // Naive exhaustive approach here.
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t = 1;
    // cin >> t;  // uncomment if spec.multitest == true
    for (int tc = 1; tc <= t; tc++) Solve();
    return 0;
}