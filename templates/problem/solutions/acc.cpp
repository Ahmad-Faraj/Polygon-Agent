#include <bits/stdc++.h>
using namespace std;

// <<<PF_PLACEHOLDER>>>
// Global typedefs / constants here if needed.
// e.g.: using ll = long long;  const int MOD = 1e9+7;

void Solve() {
    // <<<PF_PLACEHOLDER>>>
    // Read input and write output for ONE test case.
    // Use "
" not endl.
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t = 1;
    // cin >> t;  // uncomment if spec.multitest == true
    for (int tc = 1; tc <= t; tc++) {
        // cout << "Case #" << tc << ": ";  // uncomment if problem requires it
        Solve();
    }
    return 0;
}
