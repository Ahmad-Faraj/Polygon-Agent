% <<<PF_PLACEHOLDER>>>
% Write a SHORT, CLEAR story (3-5 sentences max).
% - Describe WHAT to compute, not HOW (hide the algorithm).
% - All variables in math mode: $n$, $a_i$.
%   Use \leq \geq \neq \times \ldots  (never <=, >=, x, ...)
% - End with one crisp task sentence.
% Ref: https://polygon.codeforces.com/docs/statements-tex-manual
