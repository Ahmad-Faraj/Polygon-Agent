Print two integers $i$ and $j$ ($1 \leq i < j \leq n$, $i \neq j$) such that $a_i + a_j = \text{target}$.

If no such pair exists, print \t{-1 -1}.

If multiple valid pairs exist, you may print any one of them.
